<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NetworkGuardian.App.Views.EthernetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="以太网" Style="{StaticResource PageTitle}" />
            <TextBlock Text="{x:Bind ViewModel.Ethernet.Summary, Mode=OneWay}"
                       Style="{StaticResource BodyText}"
                       Foreground="{ThemeResource AppTextSecondary}" />
            <TextBlock Text="只读展示链路、地址、网关与探测结果；默认不修改任何路由或接口度量。"
                       Style="{StaticResource BodyText}"
                       Foreground="{ThemeResource AppTextSecondary}"
                       Margin="0,4,0,12" />
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="物理以太网接口" Style="{StaticResource SectionTitle}" />
                        <ItemsControl ItemsSource="{x:Bind ViewModel.Ethernet.EthernetInterfaces, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Padding="4" Spacing="2">
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource BodyText}" />
                                        <TextBlock Text="{Binding LinkState}" Style="{StaticResource MonoText}" />
                                        <TextBlock Text="{Binding Probe}" Style="{StaticResource MonoText}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="接口详细信息" Style="{StaticResource SectionTitle}" />
                        <ScrollViewer HorizontalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Grid ColumnSpacing="16" Padding="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="100" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="名称" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="1" Text="类型" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="2" Text="链路" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="3" Text="探测" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="4" Text="IPv4" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="5" Text="网关" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="6" Text="度量" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="7" Text="默认路由" FontWeight="SemiBold" />
                                </Grid>
                                <ItemsControl ItemsSource="{x:Bind ViewModel.Ethernet.AllInterfaces, Mode=OneWay}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid ColumnSpacing="16" Padding="4,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="220" />
                                                    <ColumnDefinition Width="90" />
                                                    <ColumnDefinition Width="90" />
                                                    <ColumnDefinition Width="150" />
                                                    <ColumnDefinition Width="190" />
                                                    <ColumnDefinition Width="150" />
                                                    <ColumnDefinition Width="160" />
                                                    <ColumnDefinition Width="100" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding Name}" TextWrapping="Wrap" />
                                                <TextBlock Grid.Column="1" Text="{Binding Kind}" />
                                                <TextBlock Grid.Column="2" Text="{Binding LinkState}" />
                                                <TextBlock Grid.Column="3" Text="{Binding Probe}" />
                                                <TextBlock Grid.Column="4" Text="{Binding Ipv4}" TextWrapping="Wrap" />
                                                <TextBlock Grid.Column="5" Text="{Binding Gateway}" />
                                                <TextBlock Grid.Column="6" Text="{Binding Metrics}" />
                                                <TextBlock Grid.Column="7" Text="{Binding IsDefaultRoute}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="DNS 与 MAC" Style="{StaticResource SectionTitle}" />
                        <ItemsControl ItemsSource="{x:Bind ViewModel.Ethernet.AllInterfaces, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Padding="4,2">
                                        <TextBlock Text="{Binding Description}" Style="{StaticResource BodyText}" />
                                        <TextBlock Text="{Binding Dns}" Style="{StaticResource MonoText}" />
                                        <TextBlock Text="{Binding Mac}" Style="{StaticResource MonoText}" />
                                        <TextBlock Text="{Binding Id}" Style="{StaticResource MonoText}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="物理以太网设备（PnP）" Style="{StaticResource SectionTitle}" />
                        <ItemsControl ItemsSource="{x:Bind ViewModel.Ethernet.EthernetDevices, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Padding="4,2">
                                        <TextBlock Text="{Binding Summary}" Style="{StaticResource BodyText}" />
                                        <TextBlock Text="{Binding DetailLine}" Style="{StaticResource MonoText}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
